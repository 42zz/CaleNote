import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Text("CaleNote")
                .font(.largeTitle)
                .padding()
            
            Text("アプリをリセットしました")
                .font(.body)
                .foregroundColor(.secondary)
        }
    }
}
